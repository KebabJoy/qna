$("#link-<%= @link.id %>").remove();

<% if @linkable.links.empty? %>
  <% if @linkable.is_a?(Answer) %>
    $(".answers .links").remove();
  <% else %>
    $(".question .links").remove();
  <% end %>
<% end %>
