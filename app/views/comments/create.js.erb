document.querySelector('.comment-errors').innerHTML = '<%= render 'shared/errors', resource: @comment %>'

<% if @comment.persisted? %>
  document.querySelector('#commentable-'+'<%= @comment.commentable.id %>' +' .comments').innerHTML += '<%= j render @comment %>'
  $('#commentable-'+'<%= @comment.commentable.id %>' + ' .new-comment #comment_body').val('')
<% end %>
